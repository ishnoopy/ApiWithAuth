{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "JwtIssuer": "apiWithAuthBackend",
  "JwtAudience": "apiWithAuthBackend",
  "JwtKey": "!SomethingSecret!",

  // DOCU: Object for connecting to the MySQL workbench (the db used here is the dockerized one), accessed in Program.cs.
  "ConnectionStrings": {
    "DefaultConnection": "server=localhost; port=3307; user=root; password=password123; database=jwt_demo"
  }
}
